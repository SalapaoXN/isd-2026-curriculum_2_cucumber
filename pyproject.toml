[project]
name = "curriculum-cucumber-rebuild"
version = "1.0.0"
description = "Clean rebuild of the curriculum OCR -> RAG pipeline (behavior-preserving)."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
